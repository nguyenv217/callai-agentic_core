[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "callai-agentic_core-handlers"
version = "0.1.0"
description = "Smart handlers for callai-agentic_core"
requires-python = ">=3.10"
dependencies = [
    "callai-agentic_core>=0.6.5"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
